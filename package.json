{
  "name": "Brief_blog_en_JAMStack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
    "scripts": {
    "clean": "rm -rf ./_site",
    "build": "npm run clean && eleventy",
    "start": "npm run clean && eleventy --serve --watch"
  }
}
